import React, { useState } from 'react';


//need to call back end route

// need to get user text

//need to display generated image

// example >

return (
    <div>
     
    
      {imageUrl && <img src={imageUrl} alt="Generated Image" />}
    </div>
  );

export default DeepAIComponent;